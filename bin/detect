#!/usr/bin/env bash
echo $1
if [ -f $1/app/Gemfile ]; then
	GEMFILE=$(ls | grep Gemfile)
	GEMLEN=${#GEMFILE}
	if [ $GEMLEN -ne 0 ]; then
		echo "RTesseractFramework"
		exit 0
	else
		exit 1
	fi
else
	exit 1
fi
